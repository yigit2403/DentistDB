@model DentistDB.Models.Scan
@{
    ViewData["Title"] = "Tarama Incele";
    var fileUrl = "~/" + Model.StoredPath.Replace("\\", "/");
    var isPdf = Model.ContentType == "application/pdf";
    var isImage = Model.ContentType.StartsWith("image/");
}

<div class="page-header">
    <div>
        <div class="eyebrow">Tarama Goruntuleyici</div>
        <h2 class="page-title">@Model.FileName</h2>
        <p class="page-subtitle">@Model.Patient?.FullName - @Model.ScanDate.ToString("D") - <span class="badge-soft info">@Model.ScanType.GetDisplayName()</span></p>
    </div>
    <div class="d-flex gap-2 flex-wrap">
        <a href="@Url.Content(fileUrl)" download="@Model.FileName" class="btn btn-outline-primary">Indir</a>
        <a asp-controller="Patients" asp-action="Details" asp-route-id="@Model.PatientId" class="btn btn-outline-secondary">Hasta Dosyasina Don</a>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(Model.Notes))
{
    <div class="alert alert-light">
        <strong>Not:</strong> @Model.Notes
    </div>
}

<div class="card">
    <div class="card-body text-center p-3">
        @if (isImage)
        {
            <img src="@Url.Content(fileUrl)" alt="@Model.FileName" class="img-fluid rounded" style="max-height: 80vh;" />
        }
        else if (isPdf)
        {
            <iframe src="@Url.Content(fileUrl)" style="width:100%; height:80vh; border:none;"></iframe>
        }
        else
        {
            <p class="empty-state mb-0">Onizleme kullanilamiyor. Dosyayi indirerek inceleyebilirsiniz.</p>
        }
    </div>
</div>

<div class="mt-4">
    <form asp-controller="Scans" asp-action="Delete" asp-route-id="@Model.Id" method="post" onsubmit="return confirm('Bu taramayi kalici olarak silmek istiyor musunuz?')">
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-outline-danger">Taramayi Sil</button>
    </form>
</div>
